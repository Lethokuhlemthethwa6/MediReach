# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration (Replace with your MongoDB Atlas connection string)
MONGODB_URI=mongodb://localhost:27017/medireach

# JWT Configuration (Generate a secure random string)
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRE=7d

# Email Configuration (NodeMailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=MediReach <noreply@medireach.com>

# Frontend URL (Update with your deployed Vercel URL)
FRONTEND_URL=http://localhost:5173

# Cron Schedule (for reminder emails)
# Format: minute hour day month weekday
# Default: Every day at 9 AM
REMINDER_CRON_SCHEDULE=0 9 * * *
